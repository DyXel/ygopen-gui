#ifndef YGOPEN_SERVICE_CONFIG_HPP
#define YGOPEN_SERVICE_CONFIG_HPP

namespace YGOpen::Service
{

// Information that shall be loaded from particular files and are
// reused through the program's lifetime. Default settings should be provided
// for all variables and the program is expected to fallback to them in
// lieau of custom settings.
class Config
{
// TODO
};

} // namespace YGOpen::Service

#endif // YGOPEN_SERVICE_CONFIG_HPP
